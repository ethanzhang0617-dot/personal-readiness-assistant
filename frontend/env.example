# Copy to .env.local (gitignored) when you need to point the app at another API.
#
# Public backend URL only. Never place an API key or any secret here: Next.js
# inlines every NEXT_PUBLIC_* value into the browser bundle.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
