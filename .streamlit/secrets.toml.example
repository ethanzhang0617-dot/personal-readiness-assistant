# Every setting is optional. Do not commit a real secrets.toml file.
# Without any setting, Demo Mode and browser-local My Local Data both work.

# Disable optional Qwen loading while preserving deterministic Coach answers.
# DISABLE_EMBEDDED_LLM = "true"
